#include "raylib.h"
#include "settings.h"
#include <math.h>

void bullets(cfg *settings)
{
    // Player bullets
    //----------------------------------------------------------------------------------------
    // if (IsKeyPressed(KEY_SPACE))
    // {
    //     settings->player_bullet_pos = (Rectangle){
    //         settings->player_pos.x + settings->player_pos.width / 2,
    //         settings->player_pos.y + settings->player_pos.height / 2,
    //         10,
    //         10
    //         };
    //     settings->player_bullet_rotation = settings->player_rotation; 

    //     settings->player_bullet_pos.x += settings->player_bullet_speed * cos(settings->player_bullet_rotation);
        
    //     settings->player_bullet_pos.y += settings->player_bullet_speed * sin(settings->player_bullet_rotation);
    // }
    //     DrawRectangleRec(settings->player_bullet_pos, WHITE);
    
    //----------------------------------------------------------------------------------------
}